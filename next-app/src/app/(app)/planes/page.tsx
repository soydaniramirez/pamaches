import Stub from '@/components/Stub';

export default function Page() {
  return <Stub titulo="planes pamaches" nota="Migrar: cargarPlanes, generarIdea (moods), guardarIdeaComoPlan, togglePlan, savePlan." />;
}
