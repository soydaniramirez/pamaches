import Stub from '@/components/Stub';

export default function Page() {
  return (
    <Stub
      titulo="lista de deseos"
      nota="En construcción · se porta aparte por su gating a ciegas (solo ves los del otro si tú ya escribiste alguno). Tabla: spicy_deseos."
    />
  );
}
